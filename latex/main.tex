\documentclass[a4paper, 12pt]{article}
\usepackage[left=2cm,right=2cm]{geometry}
\usepackage{times}
\usepackage[numbers]{natbib}
\usepackage[french]{babel}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{xcolor}
\usepackage{hyperref}

% listings support
\usepackage{listings}
\usepackage{verbatim}
\lstset{
  language=Java,
  extendedchars=true,
  inputencoding=utf8,
  basicstyle=\ttfamily,
  literate=
    {à}{{\`a}}1 {â}{{\^a}}1 {ç}{{\c{c}}}1 {é}{{\'e}}1 {è}{{\`e}}1
    {ê}{{\^e}}1 {ë}{{\"e}}1 {î}{{\^i}}1 {ï}{{\"i}}1 {ô}{{\^o}}1
    {û}{{\^u}}1 {ü}{{\"u}}1 {Ç}{{\c{C}}}1 {É}{{\'E}}1 {È}{{\`E}}1
    {Ê}{{\^E}}1 {Ë}{{\"E}}1 {Î}{{\^I}}1 {Ï}{{\"I}}1 {Ô}{{\^O}}1
    {Û}{{\^U}}1 {Ü}{{\"U}}1,
}

% to make nice graph figures
\usepackage{tikz}
\usetikzlibrary{shapes,arrows}
\usetikzlibrary{arrows.meta}

% formatting specifics
\setlength{\parindent}{0pt} % sets default to \noindent
% use \hspace{1em} for indentation
\usepackage{multicol}

% basic math package
\usepackage{amssymb}
\usepackage{amsmath}

% bibliography
\usepackage[numbib, nottoc]{tocbibind}
\bibliographystyle{plainnat}
\newcommand{\figref}[1]{(fig.\ref{#1}, p.\pageref{#1})}

% for figures and images
\usepackage{float}
\usepackage{graphicx}
\usepackage{graphbox}
\usepackage{subfig} %use subcaption for better compatibility

% define command for bold underlined text
\newcommand{\bfu}[1]{\textbf{\underline{#1}}}

\title{TITLE}
\author{AUTHOR}
\date{\today}

\begin{document}
% \maketitle
% \tableofcontents

% 10 pages rapport total en anglais
% intro D
% contexte mathématique & notations (1 page pour les bases, pas Hawkes) P

% présentation de Hawkes
% noyau exp et influence alpha/beta
% calcul d'espérance et npc W

% simulation
% algorithme thinning
% étude numérique pour confirmer l'influence de alpha/beta (voir différents cas) P

% comparaison Hawkes et Cramér Lundberg
% étude numérique de la proba de ruine (pour les deux) D

% ouverture vers modèles plus généraux et bonus en annexe

% \clearpage
% \section{SECTION1}

\section{Background and Notations}
\subsection{Poisson Processes}

\bfu{Definition 1.} A counting process $N = (N(t))_{t \geq 0}$ is a sequence of non-negative random variables such that:
\begin{itemize}
    \item $N(0) = 0$,
    \item $N(t)$ takes values in the set of non-negative integers,
    \item $N(t)$ is non-decreasing and right-continuous.
\end{itemize}

\bfu{Definition 2.} A Poisson process with rate $\lambda > 0$ is a counting process $N = (N(t))_{t \geq 0}$ such that:
\begin{itemize}
    \item $N(0) = 0$,
    \item The process has independent increments,
    \item The number of events in any interval of length $t$ follows a Poisson distribution with parameter $\lambda t$.
\end{itemize}

% homogenous, non-homogenous


\subsection{Conditional Poisson Processes}

\bfu{Definition 3.} A conditional Poisson process is a counting process $N = (N(t))_{t \geq 0}$ such that, given a stochastic intensity function $\lambda(t)$, the process behaves like a Poisson process with rate $\lambda(t)$ at time $t$.


\subsection{Cramér-Lundberg Model}

As an insurance company, the main goal is to accurately model the risk associated with claim occurrences and sizes. Doing so enables the company to set appropriate premiums, maintain sufficient reserves, and ensure long-term profitability, i.e.,
a growing surplus. \medskip

We formally define the surplus as follows:
\begin{equation}
R(t) = u + ct - S(t),
\end{equation}
with $u$ the initial reserve, $c$ the premium rate, and $S(t)$ the aggregate claims up to time $t$. \medskip

In order to model $S(t)$, we consider the classical Cramér-Lundberg model which assumes that:
\begin{equation}
S(t) = \sum_{i=1}^{N(t)} X_i,
\end{equation}
where $N(t)$ is a Poisson process with rate $\lambda > 0$, representing the number of claims up to time $t$. The $X_i$ are i.i.d. random variables representing the claim sizes, \textbf{independent} of $N(t)$, with common distribution $F_X$. \medskip


% \clearpage
% \section{SECTION2}


% \clearpage
% \appendix
% \section{APPENDIX}


\end{document}
